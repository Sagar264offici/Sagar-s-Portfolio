# vgpu declares three >=0.180 as a peerOptional dep while this project pins
# three 0.170 for @react-three/drei v9. Allow the (harmless) mismatch so plain
# `npm install` — including Vercel's build step — resolves cleanly.
legacy-peer-deps=true
